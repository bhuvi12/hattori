#include<stdio.h>
#include<conio.h>
void main()
{
int x,y,z;
printf("\n ENTER THE VALUE OF x,y,z");
scanf("%d%d%d%d",&x,&y,&z);
if((x>y)&&(x>z))
{
printf("\n %d is larger than%d and %d",&x&y&z);
}
if((y>x)&&(y>z))
{
printf("\n %d is larger than%d and %d",&y&x&z);
}
if((z>x)&&(z>b))
{
printf("\n %d is larger than %d ana %d",&z&y&x);
}
return(0);
}
