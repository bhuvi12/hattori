#include<stdio.h>
int main();
{
char ch;
printf("enter the name:");
scanf("%c",&ch);
if(ch='a'//ch='e'//ch='i'//ch='o'//ch='u')
printf("%c is a vowel:\n",ch);
else
printf("not a vowel",ch);
return 0;
}
